# Dependency source of truth is setup.py. Run from the PySpotObserver directory.
# torch==2.8.0+cu129 is not on PyPI; this index exposes the CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cu129

-e .[dev]

# Vision pipeline support is optional because it requires ONNX Runtime.
# Install it when needed with:
#   pip install -e ".[vision]"

# Optional dependencies
# For development
pytest>=7.4.0                # Testing framework
pytest-asyncio>=0.21.0       # Async test support
ruff>=0.15.2                 # Code formatting and linting
mypy>=1.5.0                  # Type checking
